<table class="table table-bordered">
    <thead>
    <tr>
        <th style="font-weight: bold; text-align: center;">Nom</th>
        <th colspan="2" style="font-weight: bold; text-align: center;">Qualité de l'air</th>
    </tr>
    </thead>
    {% for station in stations %}
        <tr>
            <td>
                {{ station.nom }}
            </td>
            <td style="display: flex; align-items: center; justify-content: space-evenly;">
                {% if station.indice %}
                    {{ station.indice.label }}
                {% endif %}
            </td>
            <td>
                {% if station.indice %}
                {% include 'capteur/_color.html.twig' with {'indice': station.indice} %}
             {% endif %}
            </td>
        </tr>
    {% endfor %}
</table>