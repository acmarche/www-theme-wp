{% if event.medias | length > 1 %}
    <div class="row">
        {% for media in event.medias %}
            <div class="col-3">
                <img loading="lazy" src="{{ media.url }}" alt="{{ media.titre }}" class="img-lightGallery">
            </div>
        {% endfor %}
    </div>
{% endif %}
