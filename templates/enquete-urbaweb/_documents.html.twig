<h4 class="mt-3"><strong>Document(s)</strong></h4>
<ul class="list-group">
    {% for document in documents %}
        <li class="list-group-item">
            <a href="/download.php?permisId={{ permis.id }}&documentId={{ document.id }}">
                {{ document.libelle }}
            </a>
        </li>
    {% endfor %}
</ul>
